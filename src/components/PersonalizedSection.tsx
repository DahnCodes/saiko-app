import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { useAuth } from '../context/AuthContext.tsx'
import { getPersonalizedRecommendations } from '../services/recommendationService.ts'
import type { PersonalizedRecommendation } from '../services/recommendationService.ts'
export default function PersonalizedSection() { const { user, profile } = useAuth(); const [items, setItems] = useState<PersonalizedRecommendation[]>([]); useEffect(() => { if (user && profile?.onboardingCompleted) getPersonalizedRecommendations(user.id, 6).then(setItems).catch(() => setItems([])) }, [user, profile]); if (!user || !profile?.onboardingCompleted || !items.length) return null; return <section className="home-section"><div className="section-heading"><div><p className="eyebrow">Welcome back, @{profile.username}</p><h2>For you</h2></div><Link className="section-link" to="/recommendations">View all</Link></div><div className="anime-grid">{items.map(({ anime, reason }) => <Link className="anime-card" to={`/anime/${anime.id}`} key={anime.id}>{anime.imageUrl && <img src={anime.imageUrl} alt={`${anime.title} cover`} loading="lazy" />}<div className="anime-card-body"><h3>{anime.title}</h3><p className="anime-meta">{anime.type ?? 'Anime'} {anime.score ? `· ${anime.score.toFixed(1)}` : ''}</p><p className="anime-genres">{reason}</p></div></Link>)}</div></section> }
