import { Link, useNavigate } from 'react-router-dom'
import { useAuth } from '../context/AuthContext.tsx'
import { requireSupabase } from '../services/authService.ts'
import './profile.css'
function Avatar({ username, avatarUrl }: { username?: string; avatarUrl?: string | null }) { const initials = (username?.slice(0, 2) || 'S').toUpperCase(); return avatarUrl ? <img className="profile-page-avatar" src={avatarUrl} alt="" /> : <span className="profile-page-avatar profile-avatar-fallback" aria-hidden="true">{initials}</span> }
export default function ProfilePage() { const { user, profile, loading } = useAuth(); const navigate = useNavigate(); async function logout() { await requireSupabase().auth.signOut(); navigate('/') } if (loading) return <section className="profile-page"><div className="profile-skeleton" /></section>; if (!user) return <section className="placeholder"><p className="eyebrow">SAIKO account</p><h1>Make your anime taste count.</h1><p>Sign in or create an account to save favorites, decode your Anime DNA, and get recommendations built for you.</p><div className="hero-actions"><Link className="primary-action" to="/auth?mode=signup">Create account</Link><Link className="secondary-action" to="/auth">Sign in</Link></div></section>; return <section className="profile-page"><div className="profile-identity"><Avatar username={profile?.username} avatarUrl={profile?.avatarUrl} /><p className="eyebrow">Your SAIKO identity</p><h1>@{profile?.username ?? 'Choose a username'}</h1><p>Your favorites power your personalized discovery.</p></div><div className="profile-actions"><Link className="primary-action" to="/anime-dna">View your Anime DNA</Link><button className="secondary-action" onClick={logout}>Sign out</button></div></section> }
