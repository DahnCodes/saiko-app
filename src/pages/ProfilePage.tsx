import { Link, useNavigate } from 'react-router-dom'
import { useAuth } from '../context/AuthContext.tsx'
import { requireSupabase } from '../services/authService.ts'
export default function ProfilePage() { const { user, profile, loading } = useAuth(); const navigate = useNavigate(); async function logout() { await requireSupabase().auth.signOut(); navigate('/') } if (loading) return <section className="placeholder"><p className="eyebrow">SAIKO account</p><h1>Loading your profile...</h1></section>; if (!user) return <section className="placeholder"><p className="eyebrow">SAIKO account</p><h1>Make your anime taste count.</h1><p>Sign in or create an account to save favorites, decode your Anime DNA, and get recommendations built for you.</p><div className="hero-actions"><Link className="primary-action" to="/auth?mode=signup">Create account</Link><Link className="secondary-action" to="/auth">Sign in</Link></div></section>; return <section className="placeholder"><p className="eyebrow">Your profile</p><h1>@{profile?.username ?? user.email ?? 'Member'}</h1><p>Your favorites power your personalized discovery.</p><div className="hero-actions"><Link className="primary-action" to="/anime-dna">View Anime DNA</Link><button className="secondary-action" onClick={logout}>Sign out</button></div></section> }
