import { useEffect, useState } from 'react'
import { getTopAnime } from '../services/animeService.ts'
import type { Anime } from '../types/anime.ts'
import '../anime.css'
import { Link } from 'react-router-dom'
export default function AnimePage() { const [anime, setAnime] = useState<Anime[]>([]); const [status, setStatus] = useState<'loading' | 'success' | 'error'>('loading'); useEffect(() => { getTopAnime().then((items) => { setAnime(items); setStatus('success') }).catch(() => setStatus('error')) }, []); return <section className="anime-page"><div className="section-heading"><div><p className="eyebrow">The watchlist starts here</p><h1>Top anime</h1></div><p className="section-intro">A living shelf of the series and films shaping the conversation.</p></div>{status === 'loading' && <div className="anime-grid">{Array.from({ length: 6 }, (_, index) => <div className="anime-card skeleton" key={index} />)}</div>}{status === 'error' && <div className="state-panel"><h2>Could not load anime</h2><p>We could not load anime right now. Please try again shortly.</p></div>}{status === 'success' && anime.length === 0 && <div className="state-panel"><h2>No anime found</h2><p>There is nothing to show yet.</p></div>}{status === 'success' && anime.length > 0 && <div className="anime-grid">{anime.map((item) => <Link className="anime-card" to={`/anime/${item.id}`} key={item.id}>{item.imageUrl && <img src={item.imageUrl} alt={`${item.title} cover`} loading="lazy" />}<div className="anime-card-body"><p className="anime-rank">#{item.id}</p><h2>{item.title}</h2><p className="anime-meta">{item.type ?? 'Anime'} {item.score ? `· ${item.score.toFixed(1)}` : ''}</p></div></Link>)}</div>}</section> }
