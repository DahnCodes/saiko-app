import { useState } from 'react'
import { useNavigate, useSearchParams } from 'react-router-dom'
import { signIn, signInWithGoogle, signUp } from '../services/authService.ts'
import './auth.css'
export default function AuthPage() { const navigate = useNavigate(); const [params] = useSearchParams(); const [mode, setMode] = useState<'signin' | 'signup'>(params.get('mode') === 'signup' ? 'signup' : 'signin'); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [username, setUsername] = useState(''); const [error, setError] = useState(''); const [notice, setNotice] = useState(''); const [busy, setBusy] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setBusy(true); setError(''); setNotice(''); try { if (mode === 'signup') { const result = await signUp(email, password, username); if (result.confirmationRequired) { setNotice(`We sent a confirmation email to ${email.trim().toLowerCase()}. Confirm your address, then sign in to finish setting up your SAIKO profile.`); return } } else await signIn(email, password); navigate('/') } catch (e) { setError(e instanceof Error && (e.message.includes('profiles_username') || e.message.includes('duplicate') || e.message.includes('unique')) ? 'That username is already taken. Try another one.' : e instanceof Error ? e.message : 'Authentication failed') } finally { setBusy(false) } } async function google() { setError(''); setNotice(''); setBusy(true); try { await signInWithGoogle() } catch (e) { setError(e instanceof Error ? e.message : 'Google sign-in failed'); setBusy(false) } } return <section className="auth-page"><div className="auth-panel"><p className="eyebrow">SAIKO account</p><h1>{mode === 'signup' ? 'Create your account' : 'Welcome back'}</h1><button className="google-button" onClick={google} disabled={busy}>Continue with Google</button><div className="auth-divider">or</div><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={(e) => setEmail(e.target.value)} /></label><label>Password<input type="password" required minLength={8} value={password} onChange={(e) => setPassword(e.target.value)} /></label>{mode === 'signup' && <label>Username<input required minLength={3} maxLength={24} pattern="[A-Za-z0-9_]+" value={username} onChange={(e) => setUsername(e.target.value)} /></label>}<button className="primary-action" disabled={busy}>{busy ? 'Please wait...' : mode === 'signup' ? 'Create account' : 'Sign in'}</button></form>{notice && <p className="auth-notice" role="status">{notice}</p>}{error && <p className="auth-error" role="alert">{error}</p>}<button className="auth-switch" onClick={() => { setMode(mode === 'signup' ? 'signin' : 'signup'); setError(''); setNotice('') }}>{mode === 'signup' ? 'Already have an account? Sign in' : 'Create an account'}</button></div></section> }
