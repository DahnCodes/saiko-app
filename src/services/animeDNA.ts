import type { Anime } from '../types/anime.ts'
export type AnimeDNATrait = { name: string; score: number; icon: string }
export type AnimeDNA = { userId: string; archetype: { id: string; name: string; description: string; icon: string }; description: string; traits: AnimeDNATrait[]; favoriteAnime: Anime[]; version: number; generatedAt: string }
const traits = [['Action','⚔️'],['Adventure','🌍'],['Comedy','☀️'],['Romance','💞'],['Drama','🎭'],['Fantasy','✨'],['Sci-Fi','🚀'],['Supernatural','🌑'],['Psychological','🧠'],['Horror','🕯️'],['Sports','🏆'],['Mystery','🔎'],['Thriller','⚡'],['Slice of Life','🌿']] as const
const archetypes = [{id:'battle-explorer',name:'THE BATTLE-HARDENED EXPLORER',icon:'⚔️',description:'You love intense battles, massive adventures and characters who grow through impossible challenges.'},{id:'master-strategist',name:'THE MASTER STRATEGIST',icon:'🧠',description:'You are drawn to layered mysteries, psychological tension and stories that reward close attention.'},{id:'emotional-traveler',name:'THE EMOTIONAL TRAVELER',icon:'💞',description:'You connect with heartfelt relationships, human growth and stories that stay with you.'},{id:'world-builder',name:'THE WORLD BUILDER',icon:'✨',description:'You love imaginative worlds, rich mythology and stories that invite deep exploration.'}]
export function calculateAnimeDNA(userId: string, favorites: Anime[]): AnimeDNA { const counts = new Map<string, number>(); favorites.forEach((a) => a.genres.forEach((g) => counts.set(g, (counts.get(g) ?? 0) + 1))); const max = Math.max(...counts.values(), 1); const dnaTraits = traits.map(([name, icon]) => ({ name, icon, score: Math.round(((counts.get(name) ?? 0) / max) * 100) })).filter((t) => t.score > 0).sort((a,b) => b.score-a.score).slice(0,8); const archetype = [...archetypes].sort((a,b) => { const score=(x: typeof a) => dnaTraits.reduce((s,t)=>s+(x.id==='battle-explorer'&&['Action','Adventure'].includes(t.name)?t.score:x.id==='master-strategist'&&['Psychological','Mystery','Thriller'].includes(t.name)?t.score:x.id==='emotional-traveler'&&['Drama','Romance','Slice of Life'].includes(t.name)?t.score:x.id==='world-builder'&&['Fantasy','Sci-Fi','Supernatural'].includes(t.name)?t.score:0),0); return score(b)-score(a) })[0] ?? archetypes[0]; return {userId,archetype,description:archetype.description,traits:dnaTraits,favoriteAnime:favorites,version:1,generatedAt:new Date().toISOString()} }
